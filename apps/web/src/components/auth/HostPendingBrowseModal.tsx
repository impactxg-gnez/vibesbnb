'use client';

/**
 * Legacy: previously prompted host_pending users while browsing.
 * Hosts are auto-promoted to full host in syncProfileFromAuthUser + AuthContext; no UI needed.
 */
export function HostPendingBrowseModal() {
  return null;
}
