/**
 * Property statuses shown on public host/user profile pages.
 * Listings go live without admin approval; only bookable/active inventory is shown publicly.
 */
export const PUBLIC_HOST_PROFILE_PROPERTY_STATUSES = ['active'] as const;
